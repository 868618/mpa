// const { ENV } = process.env
console.log('process.env------------------------', process.env)

const map = new Map([
//   ['dev', 'http://local.sldshop.com'],
  ['development', '/api/'],
])

export default map
